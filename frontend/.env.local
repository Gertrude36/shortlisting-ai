# frontend/.env.local — LOCAL DEVELOPMENT
# This file overrides .env when running locally (npm run dev)
# Never commit this file to GitHub

# Point to your local backend — this DISABLES the wake banner automatically
VITE_API_URL=http://localhost:8000
