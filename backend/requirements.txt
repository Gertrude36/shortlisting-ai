fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1
python-jose[cryptography]==3.3.0

# ── Auth / password hashing ───────────────────────────────────
# IMPORTANT: bcrypt must be 4.0.1 — passlib 1.7.4 breaks with bcrypt >= 4.1
# (AttributeError: module 'bcrypt' has no attribute '__about__')
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

python-multipart==0.0.9
joblib==1.4.2
scikit-learn==1.5.0
xgboost==2.0.3
pandas>=2.2.3
numpy>=2.0.0
python-dotenv==1.0.1
opencv-python-headless==4.13.0.92

# ── Document processing ───────────────────────────────────────
Pillow==10.3.0
pytesseract==0.3.10
pdfplumber==0.11.0
pdf2image==1.17.0
pypdf==4.2.0
pymupdf==1.24.5

# ── HTTP client (for AI integrations) ────────────────────────
httpx>=0.27.0

# ── AI semantic matching ──────────────────────────────────────
# torch is installed separately in render-build.sh (CPU-only wheel)
# to avoid the huge default CUDA build (~2.5 GB).
sentence-transformers==2.7.0

# NOTE: email is sent via Brevo HTTPS API using Python stdlib urllib only.
# No SMTP, no extra packages needed.